import React from 'react';
import Slider from '../slider/Slider';

const Homepage = () => {
    return (
        <div>
            <Slider></Slider>
            <h1>hi</h1>
        </div>
    );
};

export default Homepage;